{
  "name": "bulletin-backup",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Nightly off-site backup of the cloud database and uploads. Deliberately its own package, outside the npm workspaces, so the API and web bundles never carry the AWS SDK.",
  "scripts": {
    "backup": "node backup.mjs"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0"
  }
}
